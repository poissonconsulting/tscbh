# Read BC Hydro Data File

Reads in an Excel spreadsheet of BC Hydro data as provided by Power
Records. The station cannot be consistently determined from the file so
must be manually added.

## Usage

``` r
ts_read_bch(file = "tscbh.xls")
```

## Arguments

- file:

  A string specifying the path to the file.
